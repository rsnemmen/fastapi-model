# --- Web framework & server ---
fastapi>=0.110          # 0.110 introduced Pydantic-v2 support
uvicorn[standard]>=0.29  # ASGI server used by FastAPI

# --- Data / ML stack ---
numpy>=1.23              # 1.23+ is broadly compatible with recent pandas / sklearn
pandas>=2.0              # 2.x for Python ≥3.8, stays clear of upcoming 3.x changes
scikit-learn>=1.3        # 1.3+ still loads joblib models safely
joblib                   # Serialization / deserialization
xgboost

# --- Validation ---
pydantic>=2.5            # FastAPI (≥0.110) relies on Pydantic v2

# --- Typing back-ports (needed for some Python versions) ---
typing-extensions